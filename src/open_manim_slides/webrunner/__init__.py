"""Local web runner: browse decks, render with a live progress bar, and
present the result in-browser. Optional add-on, not imported by the core
package -- install with `pip install -e ".[web]"` and run via
`python -m open_manim_slides.webrunner`.
"""
